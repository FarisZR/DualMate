// Kept as the stable entry point for the cold-navigation profile suite.
//
// The implementation is split out so its deterministic fixtures and metric
// summaries can also receive focused automated coverage.
import 'cold_navigation_performance_test.dart' as harness;

void main() => harness.main();
